## .bashrc
    if [ -f ~/.bccrc ]; then
        . ~/.bccrc
    fi

## prerequisits
clone this so in $HOME rename .git to .dotfiles

    git clone
    cd dotfiles
    mv .git .dotfiles
    mv ./{*,.*} ../
    cd ..
    rm -r dotfiles

## use
    dotfiles st
